# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.5.0
pandas>=1.3.0

# Neural networks and machine learning
torch>=1.9.0
scikit-learn>=1.0.0

# Visualization and GUI
pygame>=2.1.0      # Required for the 2D (--gui) and isometric (--gpu) desktop renderers
moderngl>=5.8.0   # Required for the GPU isometric (--gpu) renderer
# NOTE: The Three.js web UI (--web) needs NO extra Python packages — the server
# uses only the standard library, and Three.js is loaded in the browser via CDN.

# Data processing and analysis
pyarrow>=5.0.0  # For Parquet format
h5py>=3.0.0     # For HDF5 format
openpyxl>=3.0.0 # For Excel export

# Configuration and utilities
pyyaml>=6.0
tqdm>=4.60.0
colorama>=0.4.4

# Testing
pytest>=7.0.0
pytest-cov>=3.0.0
pytest-mock>=3.0.0

# Development tools
black>=22.0.0
flake8>=4.0.0
mypy>=0.950

# Optional: Advanced visualization
seaborn>=0.11.0  # Statistical plotting
plotly>=5.0.0    # Interactive plots
bokeh>=2.4.0     # Web-based visualization

# Optional: Performance optimization
numba>=0.56.0    # JIT compilation
cython>=0.29.0   # C extensions
